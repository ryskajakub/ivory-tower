// import pgPromise from 'pg-promise';

// const pgp = pgPromise({});

// const connectionString = process.env["DATABASE_URL"] || ""

// const db = pgp(connectionString);

// const value = await db.manyOrNone('SELECT * from users');
// console.log(value)
